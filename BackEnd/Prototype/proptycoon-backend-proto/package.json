{
  "name": "proptycoon-backend-proto",
  "version": "0.1.0",
  "description": "Property Tycoon back-end prototype.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "npm run build && node dist/index.js"
  },
  "author": "AymanZenos",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "chai": "^4.1.2",
    "mocha": "^5.0.1",
    "rimraf": "^2.6.2"
  },
  "repository": "https://github.com/OwenDaughtery/Mr-Pickles.git"
}
